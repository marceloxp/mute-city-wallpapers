<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mute City Wallpapers Collection</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Press Start 2P', monospace;
            background-color: #1a1a1a;
            color: #f5f5f5;
        }
    </style>
</head>

<body class="flex flex-col items-center justify-center min-h-screen">
    <!-- Header -->
    <header class="text-center mb-8">
        <h1 class="text-4xl md:text-6xl">Mute City Wallpapers Collection</h1>
    </header>

    <!-- Gallery -->
    <main class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 w-full px-4">
        {{#each images}}
        <a href="{{this}}" download class="hover:opacity-75 transition">
            <img src="{{this}}" alt="{{this}}" class="w-full h-auto rounded-lg shadow-lg">
        </a>
        {{/each}}
    </main>
</body>

</html>